/**
 * Drag and Drop event setup for player/spectator lists.
 */

export function addDnDEvents() {
    // DnD events are set up in main.js setupDragAndDrop()
    // This module exists for backwards compatibility with import paths
}
